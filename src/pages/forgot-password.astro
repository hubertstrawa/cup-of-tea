---
import Layout from "../layouts/Layout.astro";
import { AuthForm } from "../components/AuthForm";

export const prerender = false;

// Sprawdzenie czy użytkownik jest już zalogowany
// (w przyszłości będzie to obsługiwane przez middleware)
// const { supabase } = Astro.locals;
// const { data: { session } } = await supabase.auth.getSession();
//
// if (session) {
//   return Astro.redirect('/dashboard');
// }

// Funkcja obsługująca wysyłanie linku resetującego (placeholder)
const handleForgotPassword = async () => {
  // TODO: Implementacja resetowania hasła przez Supabase Auth
  // const { error } = await supabase.auth.resetPasswordForEmail(data.email, {
  //   redirectTo: `${window.location.origin}/reset-password`
  // });

  // if (error) {
  //   throw new Error(error.message);
  // }

  // Pokazanie komunikatu o wysłaniu linku
  // alert('Link do resetowania hasła został wysłany na Twój adres e-mail.');

  // Tymczasowe zachowanie dla demonstracji
  console.log("Funkcjonalność zostanie zaimplementowana w następnym etapie.");
};
---

<Layout title="Odzyskiwanie hasła - Cup of Tea">
  <main class="min-h-screen flex items-center justify-center bg-gray-50 py-12 px-4 sm:px-6 lg:px-8">
    <div class="max-w-md w-full space-y-8">
      <div class="text-center">
        <h1 class="text-3xl font-bold text-gray-900 mb-2">Cup of Tea</h1>
        <p class="text-gray-600">System zarządzania lekcjami języków obcych</p>
      </div>

      <div class="bg-blue-50 border border-blue-200 rounded-lg p-4 mb-6">
        <div class="flex">
          <div class="flex-shrink-0">
            <svg class="h-5 w-5 text-blue-400" viewBox="0 0 20 20" fill="currentColor">
              <path
                fill-rule="evenodd"
                d="M18 10a8 8 0 11-16 0 8 8 0 0116 0zm-7-4a1 1 0 11-2 0 1 1 0 012 0zM9 9a1 1 0 000 2v3a1 1 0 001 1h1a1 1 0 100-2v-3a1 1 0 00-1-1H9z"
                clip-rule="evenodd"></path>
            </svg>
          </div>
          <div class="ml-3">
            <h3 class="text-sm font-medium text-blue-800">Odzyskiwanie hasła</h3>
            <div class="mt-2 text-sm text-blue-700">
              <p>Wprowadź adres e-mail powiązany z Twoim kontem. Wyślemy Ci link do resetowania hasła.</p>
            </div>
          </div>
        </div>
      </div>

      <AuthForm mode="forgot-password" onSubmit={handleForgotPassword} isLoading={false} client:load />
    </div>
  </main>
</Layout>
